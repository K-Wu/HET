# From sputnik/sputnik/utils/CMakeLists.txt
set(HETEROEDGESOFTMAX_INCLUDE_FILES)

##
### Find all sources in this directory.
##

file(GLOB TMP *.h)
list(APPEND HETEROEDGESOFTMAX_INCLUDE_FILES ${TMP})
file(GLOB TMP *.cc)
list(APPEND HETEROEDGESOFTMAX_INCLUDE_FILES ${TMP})

# Add the sources to the build.
set(HETEROEDGESOFTMAX_SRCS ${HETEROEDGESOFTMAX_SRCS} ${HETEROEDGESOFTMAX_INCLUDE_FILES} PARENT_SCOPE)
